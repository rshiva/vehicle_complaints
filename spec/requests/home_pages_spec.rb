require 'spec_helper'

describe "HomePages" do
  describe "GET /home_pages" do
    it "visit home and should have title vehiclecomplaints" do
      # Run the generator again with the --webrat flag if you want to use webrat methods/matchers
      #visit "/"
      #page.shoud have_selector("title",:text => "VehicleComplaints")
    end
  end
end
